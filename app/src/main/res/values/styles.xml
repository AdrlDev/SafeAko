<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Custom style for transparent background AlertDialog -->
    <style name="TransparentAlertDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item> <!-- Optional: to dim the background when dialog appears -->
    </style>
</resources>